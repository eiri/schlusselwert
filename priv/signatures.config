#{
    listen => #{ip => {0, 0, 0, 0}, port => 8080},
    probe => #{bytes => 4096, timeout => 1000},
    protocols => [
        #{
            name => http,
            upstream => #{host => "nginx", port => 80},
            signatures => [
                #{at => 0, bytes => <<"GET ">>, priority => 30},
                #{at => 0, bytes => <<"HEAD ">>, priority => 30},
                #{at => 0, bytes => <<"POST ">>, priority => 30},
                #{at => 0, bytes => <<"PUT ">>, priority => 30},
                #{at => 0, bytes => <<"DELETE ">>, priority => 30},
                #{at => 0, bytes => <<"OPTIONS ">>, priority => 30}
            ]
        },
        #{
            name => postgres,
            upstream => #{host => "postgres", port => 5432},
            signatures => [
                #{at => 0, bytes => <<0, 0, 0, 8, 4, 210, 22, 47>>, priority => 20},
                #{at => 4, bytes => <<0, 3, 0, 0>>, priority => 20},
                #{at => 4, bytes => <<4, 210, 22, 46>>, priority => 20},
                #{at => 0, bytes => <<0, 0, 0, 8, 4, 210, 22, 48>>, priority => 20}
            ]
        },
        #{
            name => redis,
            upstream => #{host => "redis", port => 6379},
            signatures => [
                #{at => 0, bytes => <<"*">>, priority => 10},
                #{at => 0, bytes => <<"$">>, priority => 10}
            ]
        }
    ]
}.
