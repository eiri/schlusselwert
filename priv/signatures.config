#{
    listen => #{ip => {0, 0, 0, 0}, port => 8080},
    probe => #{bytes => 64, timeout => 1000},
    protocols => [
        #{
            name => http,
            upstream => #{host => "nginx", port => 80},
            signatures => [#{at => 0, bytes => <<"GET ">>}]
        }
    ]
}.
